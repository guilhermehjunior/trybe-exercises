const ping = require('./ping');
const upload = require('./upload');
const multiple = require('./multiple');
const profile = require('./profile');
const profileId = require('./profileId');

module.exports = {
  ping,
  upload,
  multiple,
  profile,
  profileId,
};
